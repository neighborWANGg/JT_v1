# JT_V1 开发进度与协同交接

> 更新时间：2026-09-13  
> 当前阶段：V1 本地可运行闭环；V2 人员伴随查询未开工  
> 默认技术路线：FastAPI + SQLAlchemy + SQLite + 原生 HTML/CSS/JavaScript + LM Studio / DeepSeek 等 OpenAI 兼容接口  
> 配套文档：`docs/需求说明书.md`（要做成什么样）、`docs/协作规范.md`（怎么一起改代码）

## 0. 任务看板

| 状态 | 任务 | 需求 | 负责人 | 说明 |
| --- | --- | --- | --- | --- |
| 已完成 | V1 案件研判闭环 | V1 | — | 导入、检索、对话研判、复核、审计 |
| 已完成 | DeepSeek API 兼容 | V1 | — | API 模式使用 `json_object`，Key 去空格 |
| 进行中 | 协作文档 | — | 待认领 | 需求 / 规范 / 本进度表 |
| 未开始 | 人员主数据与线索对齐 | R1 R2 | 待认领 | 见需求说明书 |
| 未开始 | 个人档案页与导出 | R3 | 待认领 | |
| 未开始 | 关系拓扑图 | R4 | 待认领 | |
| 未开始 | 对话查询接到「人」 | R5 | 待认领 | |
| 未开始 | 登录权限与脱敏 | R7 | 待认领 | 上线内网前必做 |

每人只改自己负责任务对应的代码和测试，合并前更新本表状态和顶部日期。


## 1. 项目定位

JT_V1 是面向已授权群聊资料的辅助研判系统。系统以“案件”为数据边界，以“研判对话”为工作单元，对导入的聊天记录进行字段检索、关键词检索、向量检索、上下文扩展和结构化模型分析，并保留原始证据引用和人工复核记录。

模型输出只作为辅助分析，不替代人工结论。

## 2. 当前可运行能力

### 2.1 案件与数据管理

- 创建、查看、归档、恢复和删除案件。
- 查看案件的来源文件数、消息数、研判数和复核数。
- 查看来源资料清单和 SHA-256。
- 将案件、来源、消息、对话和研判记录导出为 JSON。
- 记录案件、资料导入、对话、研判和复核等操作审计。
- 默认使用 SQLite，不依赖独立数据库服务。

### 2.2 聊天记录导入

- 支持导入 `.xlsx`，工作表名称为 `聊天记录`。
- 必需字段：`序号`、`时间`、`群ID`、`微信号`、`聊天内容`、`IP登录地址`。
- 导入前校验文件格式和字段完整性，单文件限制为 50 MB。
- 使用文件 SHA-256 识别重复导入。
- 根据文件哈希、工作表和行号生成稳定消息 ID。
- 原始文件按案件保存，导入后的证据可回溯到文件和 Excel 行号。

### 2.3 检索与证据上下文

- 混合排序：向量相似度、中文/英文关键词重合、精确问题、账号/群/IP 字段命中和反证词命中。
- 支持按开始日期、结束日期、微信号、群 ID、IP 登录地址和来源文件过滤。
- 搜索接口与研判接口共享相同过滤逻辑，筛选后的上下文证据不会越出筛选范围。
- 支持按原始消息顺序读取前后文。
- 支持读取同群指定分钟范围内的时间邻域。
- 对“某 wxid 的登录地址是什么”提供结构化字段直查，不依赖大模型猜测。
- 当前筛选范围没有消息时直接返回“证据不足”，不调用模型。

### 2.4 模型调用

- 支持本地模型和 API 模型两种模式，由前端切换。
- 本地模式通过 LM Studio 的 OpenAI 兼容接口调用 Qwen。
- API 模式可在前端填写 Base URL、模型名和 API Key。
- API Key 只随当前请求传输，不写入数据库或浏览器存储。
- 支持通过 `API_LLM_ALLOWED_HOSTS` 限制前端可访问的 API 域名。
- 使用 JSON Schema 约束模型输出，包括结论等级、事实/推断、证据引用、反证、替代解释和置信等级。
- 校验模型返回的证据 ID，阻止引用本案证据集合之外的消息。
- 携带同一研判对话最近 5 轮摘要帮助理解追问，但历史模型结论不会被当作新证据。

### 2.5 异步研判任务

- 创建、查询和取消异步研判任务。
- 状态包括 `queued`、`running`、`succeeded`、`failed`、`cancelled`。
- 前端轮询任务状态，可在运行期间请求取消。
- 自定义 API Key 不写入任务表。

### 2.6 研判对话与人工复核

- 每个案件可创建多个研判对话。
- 支持重命名、置顶、未读、归档和分区。
- 支持复制标题/ID、内部分享链接、在新窗口打开和同案件对话分叉。
- 空对话可以移动到其他案件；已有研判记录的对话禁止跨案件移动，避免破坏证据边界。
- 删除对话时同步删除该对话的研判和复核记录，不影响案件原始资料。
- 人工复核支持采纳、修改和驳回，复核记录追加保存，不覆盖模型原始输出。
- 修改后的研判仍会校验证据引用是否属于当前案件。

### 2.7 前端工作台

- Codex 风格左侧案件/对话导航和右键菜单。
- 桌面端三栏布局：案件导航、研判对话、证据侧栏。
- 对话式展示历次问题和研判摘要，完整结论单独展示。
- 输入框固定在工作台底部，支持 `Ctrl + Enter` 提交。
- 显示异步任务的排队、运行、完成、失败和取消状态。
- 真实筛选面板支持日期、人员、群、IP 和来源文件。
- 已启用筛选条件显示为可单独移除的标签，并支持一键清空。
- 日期范围冲突会在面板内直接提示。
- 无匹配、证据不足和模型失败具有独立状态提示。
- 点击证据可查看来源文件、Excel 行号、发送账号、群 ID、IP 和前后文。
- IP 默认脱敏，可由使用者切换显示原文。
- 支持窄屏布局；核心研判和输入功能可在小窗口使用。

## 3. 主要接口

| 模块 | 接口 | 说明 |
| --- | --- | --- |
| 健康检查 | `GET /health` | 服务状态 |
| 前端 | `GET /ui` | 工作台页面 |
| 案件 | `GET /cases`、`POST /cases` | 案件列表与创建 |
| 案件 | `PATCH /cases/{case_id}` | 修改或归档案件 |
| 案件 | `DELETE /cases/{case_id}` | 删除案件和全部关联数据 |
| 案件 | `GET /cases/{case_id}/stats` | 数据规模统计 |
| 数据 | `GET/POST /cases/{case_id}/sources` | 来源清单与 Excel 导入 |
| 数据 | `GET /cases/{case_id}/filter-options` | 可用筛选项 |
| 数据 | `GET /cases/{case_id}/export` | 案件 JSON 导出 |
| 审计 | `GET /cases/{case_id}/audit-logs` | 操作审计列表 |
| 对话 | `GET/POST /cases/{case_id}/conversations` | 对话列表与创建 |
| 对话 | `PATCH/DELETE /conversations/{id}` | 更新或删除对话 |
| 对话 | `POST /conversations/{id}/fork` | 对话分叉 |
| 对话 | `GET /conversations/{id}/turns` | 历史研判轮次 |
| 检索 | `GET /cases/{case_id}/search` | 混合检索与字段过滤 |
| 上下文 | `GET /cases/{case_id}/messages/{id}/context` | 原始顺序前后文 |
| 上下文 | `GET /cases/{case_id}/messages/{id}/time-neighbors` | 同群时间邻域 |
| 研判 | `POST /cases/{case_id}/analysis` | 同步研判 |
| 任务 | `POST /cases/{case_id}/analysis-tasks` | 创建异步研判任务 |
| 任务 | `GET /analysis-tasks/{id}` | 查询任务和结果 |
| 任务 | `POST /analysis-tasks/{id}/cancel` | 请求取消任务 |
| 复核 | `GET/POST /analyses/{id}/reviews` | 复核历史与追加复核 |

完整接口结构以启动后的 `/docs` 为准。

## 4. 代码结构

```text
JT_V1/
├─ app/
│  ├─ main.py       # FastAPI 路由、研判编排、异步任务
│  ├─ models.py     # SQLAlchemy 数据模型
│  ├─ schemas.py    # 请求/响应和模型输出 Schema
│  ├─ services.py   # Excel 导入、混合检索、上下文扩展
│  ├─ llm.py        # LM Studio/API 模型调用与结果校验
│  ├─ db.py         # 数据库初始化与轻量 SQLite 迁移
│  └─ config.py     # 环境变量配置
├─ frontend/
│  └─ index.html    # 当前原生前端工作台
├─ tests/
│  ├─ test_api.py
│  ├─ test_llm.py
│  └─ eval_questions.json
├─ README.md
└─ DEVELOPMENT_PROGRESS.md
```

## 5. 测试状态

- 当前自动化测试：`10 passed`。
- 覆盖案件、导入去重、搜索、字段直查、对话操作、上下文、时间邻域、人工复核、异步任务、数据管理、审计和筛选逻辑。
- `eval_questions.json` 包含第一版标准检索问题。
- 前端已执行 JavaScript 语法检查，并使用真实案件数据完成浏览器检查。

运行测试：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

## 6. 本地启动

基础模式：

```powershell
python -m venv .venv
.\.venv\Scripts\pip.exe install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8765
```

启用 LM Studio：

```powershell
$env:LLM_ENABLED="1"
$env:LLM_BASE_URL="http://127.0.0.1:1234/v1"
$env:LLM_MODEL="qwen/qwen3.6-35b-a3b"
$env:EMBEDDING_BACKEND="lmstudio"
$env:EMBEDDING_MODEL="text-embedding-nomic-embed-text-v1.5"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8765
```

启动后访问：`http://127.0.0.1:8765/ui`。

切换 embedding 模型后，应重新导入案件资料，避免混用不同模型生成的历史向量。

## 7. 已知限制

- 异步任务由 FastAPI 进程内后台任务执行，服务重启时不保证运行中任务续跑，也不适合多实例部署。
- SQLite 检索会在应用进程内对候选消息计算向量分数，适合当前原型和中小数据量。
- SQLite 表结构升级使用内置轻量迁移代码，尚未引入 Alembic。
- 当前只支持固定格式的 Excel，不支持 CSV、JSON、字段映射或逐行错误报告。
- API 模型必须兼容 OpenAI Chat Completions 和 JSON Schema 结构化输出。
- 筛选项目前按案件全量读取并去重；超大案件需要改为数据库聚合、分页或搜索式下拉框。
- 登录、用户、角色和案件权限按当前决策暂不实现。

## 8. 待开发功能与建议顺序

### P0：进入稳定试用前

1. 扩充真实标注评测集，统计检索 Top-K 命中率、字段问答准确率、证据引用正确率和反证召回率。
2. 增加任务失败后的显式重试接口、任务历史列表，以及服务启动时对残留 `queued/running` 任务的恢复策略。
3. 增加来源文件删除与重新索引流程，并处理删除来源后关联证据和历史研判的策略。
4. 为导入失败提供行号、字段名和失败原因，支持用户下载错误清单。
5. 补充并发研判、较大 Excel、模型超时、API 错误和异常 JSON 的集成测试。

### P1：提升一线使用效率

1. 将案件管理弹窗升级为独立数据管理页面。
2. 增加研判报告导出，输出面向人工归档的 Markdown、DOCX 或 PDF；当前 JSON 导出主要用于数据交换。
3. 在窄屏下将证据侧栏改成可打开的证据抽屉。
4. 增加来源、人员和群的搜索式选择，支持多选条件。
5. 增加研判任务历史、失败原因详情和一键重试。
6. 增加复核状态汇总、待复核队列和已复核结果视图。

### P2：数据规模或多人协作出现后

1. 从 SQLite 迁移到 PostgreSQL + pgvector，并使用 Alembic 管理迁移。
2. 使用独立任务队列执行模型任务，并实现真正的任务取消、超时、重试和并发限制。
3. 增加日志、指标、备份恢复、部署配置和反向代理方案。
4. 在需求确认后实现登录、用户角色、案件授权和审计操作人身份。
5. 对 API Key 使用服务端密钥管理，不在普通业务请求中长期携带。

## 9. 协同开发注意事项

- 不要提交 `.env`、数据库文件、导入后的案件资料、本地模型或 API Key；这些路径已加入 `.gitignore`。
- 新接口应同步补充 Pydantic Schema 和最小可运行测试。
- 研判结果中的事实主张必须引用原始消息 ID；历史模型结论不能替代证据。
- 涉及案件移动、删除或来源重建时，必须保持案件数据边界和证据可追溯性。
- 当前前端不使用构建工具，修改 `frontend/index.html` 后刷新 `/ui` 即可验证。
- 单案接近 10 万条消息、出现多用户并发或多实例部署需求时，再执行 PostgreSQL、pgvector 和独立任务队列升级。
